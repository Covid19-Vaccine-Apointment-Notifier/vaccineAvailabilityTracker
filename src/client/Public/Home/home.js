import React, { Component, useState, useEffect, Fragment, useRef } from "react";

const Home = (props) => {
  let history = useHistory();

  return (<div>
    Hii home
  </div>)
};

export default Home;
